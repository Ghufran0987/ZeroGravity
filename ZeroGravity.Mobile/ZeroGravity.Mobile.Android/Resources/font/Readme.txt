In order to use custom fonts for the Android SplashScreen, the fonts have to be in the .Android project aswell.
ZeroGravity.Mobile.Android > Resources > font

Font-files can be of type .otf or .ttf

Font-files ARE NOT ALLOWED to contain a hyphen
=> Use fontFile.ttf instead of font-file.ttf for example
